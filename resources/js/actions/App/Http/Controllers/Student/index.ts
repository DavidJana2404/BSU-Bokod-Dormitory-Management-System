import InitialPasswordController from './InitialPasswordController'
import Settings from './Settings'
const Student = {
    InitialPasswordController: Object.assign(InitialPasswordController, InitialPasswordController),
Settings: Object.assign(Settings, Settings),
}

export default Student